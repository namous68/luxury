<form method="post" action="{{ path('candidature_delete', {'id': candidature.id}) }}" onsubmit="return confirm('Are you sure you want to delete this item?');">
    <input type="hidden" name="_token" value="{{ csrf_token('delete' ~ candidature.id) }}">
    <button class="btn bn-lg gradient red darken-3 btn-block waves-effect waves-light btn-register">Delete</button>
</form>